import { requireGrowthSubcontractor } from "@/lib/subcontractor-auth";
import { SUBCONTRACTOR_PAY_TYPE_LABELS, formatSubcontractorCurrency } from "@/lib/subcontractor-payroll";
export default async function Page() { const s = await requireGrowthSubcontractor(); return <div><h1 className="text-2xl font-bold">Profile</h1><p className="mt-1 text-sm text-slate-500">Contact Winsalot Corp to request a correction.</p><dl className="mt-6 grid gap-4 rounded-2xl border bg-white p-6 sm:grid-cols-2"><div><dt className="text-xs font-semibold uppercase text-slate-400">Full Name</dt><dd className="mt-1 font-medium">{s.full_name}</dd></div><div><dt className="text-xs font-semibold uppercase text-slate-400">Email</dt><dd className="mt-1 font-medium">{s.email ?? "-"}</dd></div><div><dt className="text-xs font-semibold uppercase text-slate-400">Phone</dt><dd className="mt-1 font-medium">{s.phone ?? "-"}</dd></div><div><dt className="text-xs font-semibold uppercase text-slate-400">Country</dt><dd className="mt-1 font-medium">{s.country ?? "-"}</dd></div><div><dt className="text-xs font-semibold uppercase text-slate-400">Pay Type</dt><dd className="mt-1 font-medium">{SUBCONTRACTOR_PAY_TYPE_LABELS[s.pay_type]}</dd></div><div><dt className="text-xs font-semibold uppercase text-slate-400">Rate</dt><dd className="mt-1 font-medium">{formatSubcontractorCurrency(s.pay_rate, s.currency)}</dd></div></dl></div>; }
