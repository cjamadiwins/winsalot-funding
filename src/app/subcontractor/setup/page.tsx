import { completeSubcontractorSetupAction } from "./actions";
export default async function Page({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const params = await searchParams;
  return <div className="crm-theme flex min-h-screen items-center justify-center bg-[var(--color-bg)] px-4"><div className="w-full max-w-sm rounded-2xl border bg-white p-8 shadow-sm"><p className="text-sm font-semibold text-sky-600">Winsalot Corp</p><h1 className="mt-1 text-xl font-bold">Set Up Your Portal</h1><p className="mt-1 text-sm text-slate-500">Choose a private password with at least 12 characters.</p><form action={completeSubcontractorSetupAction} className="mt-6 space-y-4"><label className="block text-sm font-medium">Password<input name="password" type="password" minLength={12} required className="mt-1.5 w-full rounded-lg border px-3.5 py-3" /></label><label className="block text-sm font-medium">Confirm Password<input name="confirm_password" type="password" minLength={12} required className="mt-1.5 w-full rounded-lg border px-3.5 py-3" /></label>{params.error && <p className="text-sm text-red-600">{params.error}</p>}<button className="w-full rounded-full bg-sky-600 px-4 py-3 font-semibold text-white">Save Password</button></form></div></div>;
}
