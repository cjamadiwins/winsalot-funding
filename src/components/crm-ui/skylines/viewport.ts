// Shared coordinate space every skyline piece is drawn in. Kept wide and
// short (matches the thin banner strip at the top of each card) - see
// CitySkyline.tsx for how this maps onto the actual rendered pixels.
export const VIEW_WIDTH = 300;
export const VIEW_HEIGHT = 84;
export const GROUND_Y = 84;
